import { PriceServiceClass } from './price-service-class';

describe('PriceServiceClass', () => {
  it('should create an instance', () => {
    expect(new PriceServiceClass()).toBeTruthy();
  });
});
